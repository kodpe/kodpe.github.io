// CONSTANTS ONLY
export const URL = Object.freeze({
  CUISINE: window.location.origin + '/' + window.location.pathname.split('/')[1] + "/kitchen.html",
  PARK: window.location.origin + '/' + window.location.pathname.split('/')[1] + '/park.html',
  DOMAIN: window.location.origin + '/' + window.location.pathname.split('/')[1] + '/park.html',
  STREET: window.location.origin + '/' + window.location.pathname.split('/')[1] + '/street.html',
});